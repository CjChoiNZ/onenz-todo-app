name: ui_elements
description: Shared UI Design System for the OneNZ To-Do App. Contains reusable Buttons, TextFields, Cards, and theme definitions.

version: 0.1.0

environment:
  sdk: ">=3.6.0 <4.0.0"
  flutter: ">=3.29.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0
